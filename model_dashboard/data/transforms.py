"""Parquet schema aliases and dataframe transforms.

The implementation is being migrated out of `model_dashboard.data_loader`.
Keep new transform logic here; compatibility wrappers remain in data_loader
until the legacy tests are retired.
"""
