# Agent State

Status: VERIFIED

Latest verified commit: d6a8bda

Active task: harden dashboard verification against hanging shell commands and
repair stale Playwright expectations so the bounded verifier can run to a
deterministic pass or first failure.

## Current Verified State

- Exact basecase workbook now opens and validates.
- Exact high_population workbook opens and validates.
- `scripts/run_forecast_pack.py` passed for both exact workbooks with
  `--end-period 2050Q4`.
- Both exact scenarios produced `numeric_forecast_available`, 300 future rows
  each, 100 positive finite forecasts for PED, Light RUC and Heavy RUC, and no
  governed gap streams.
- Scenario comparison was written to
  `test-output/tmp/a8532898e4ec4751a6604bf4b722ea33/runs_current/scenario_comparison_exact`
  with 600 future rows and `scenario_input_delta_audit` rows showing the
  high_population fixture is exactly 2% above base inputs and not
  decision-grade/population-only.
- Current model names in exact outputs:
  - PED: `PED__VNEXT_SOLVED_CONVEX_TOP2`
  - Light RUC: `dynamic_RESID_GBR_n150_d1_lr0.05_w36`
  - Heavy RUC: `HEAVY_RUC__VNEXT_SOLVED_CONVEX_TOP4`
- Component-weight reconciliation max absolute delta was
  `5.960464477539063e-08`.
- PED and Heavy vNext parity statuses were `passed`; stored replay deltas were
  within `1e-6`.
- Fresh hash guard after the current commit showed 59 evidence/chart-source
  files checked and 0 changed by forecast runs.
- Focused Playwright browser verification passed against `localhost:8511`.
- Main e2e Playwright group passed with bounded execution:
  `38 passed in 96.07s`.
- Frontend interaction Playwright suite passed with bounded execution:
  `9 passed in 57.39s`.
- Full bounded `scripts/verify_dashboard.ps1` passed with
  `-StartupTimeoutSeconds 90` and `-CommandTimeoutSeconds 900`.
- The 120-gate visual conformance suite passed, including screenshot matrix,
  filter/hover evidence, and closed `BUG_BACKLOG.md`.
- Fresh browser screenshots were saved under `artifacts/screenshots`.
- `artifacts/recursive_audit_loops.json` was generated from 21 real browser
  checks with screenshot evidence.
- BUG_BACKLOG.md: no unchecked items.

## Commands Passed

- `python -m compileall app.py model_dashboard scripts`
- `pytest -q tests/test_visual_artifacts.py tests/test_forecast_runner.py tests/test_vnext_parity.py`
- `scripts/inspect_parquet_schema.py --data-root data/dashboard_evidence_pack`
- `scripts/validate_dashboard_data.py --data-root data/dashboard_evidence_pack`
- `scripts/validate_chart_sources.py --data-root data/dashboard_evidence_pack`
- `scripts/validate_semantic_labels.py --data-root data/dashboard_evidence_pack`
- `pytest -q tests/test_recursive_audit_log.py tests/test_visual_artifacts.py`
- Full `pytest -q`: `305 passed, 49 skipped, 39 deselected`.
- `scripts/validate_80_gates.py --data-root data/dashboard_evidence_pack`:
  `100/100` gates passed with zero supporting failures.
- `scripts/verify_dashboard.ps1 -Python "C:\Users\Adrian Desilvestro\Repos\NLTF-revenue-ML-model-dashboard\.venv\Scripts\python.exe" -DataRoot data\dashboard_evidence_pack -Port 8512 -StartupTimeoutSeconds 90 -CommandTimeoutSeconds 900`
  passed through `scripts/invoke_bounded.ps1`.

## Verification Guardrail

`scripts/verify_dashboard.ps1` invokes long-running Python, pytest and
Playwright steps through `scripts/invoke_bounded.ps1`, and Streamlit startup has
its own bounded health-check helper.

## Local Scratch Notes

- `.runtime_pyarrow24/` is a scratch dependency folder and must not be
  committed.
- `.streamlit_8511.pid` is local runtime scratch and must not be committed.
- `data/dashboard_evidence_pack_reproducibility/ped_inner_hpo/source_artifacts/scripts/__pycache__/`
  is local Python cache output and must not be committed.
