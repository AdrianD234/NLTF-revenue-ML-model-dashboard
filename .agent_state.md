# Agent State

Status: IN PROGRESS
Current loop number: repo-cleanup pass 3 of 80

## Latest Verified Pass

Command:

```powershell
pwsh -File scripts\verify_dashboard.ps1 -Python ".venv\Scripts\python.exe" -DataRoot "tests\fixtures\mini_parquet" -Port 8501
```

Result: PASS.

Evidence:

- Compile passed for `app.py`, `model_dashboard`, and `scripts`.
- Full pytest passed: 104 passed, 46 skipped, 38 deselected.
- Data schema inspection passed against `tests\fixtures\mini_parquet`.
- Data validation passed against the mini Parquet fixture.
- Chart-source validation passed for all 16 primary source tables.
- Semantic-label validation passed.
- Browser e2e passed: 37 tests.
- Mandatory frontend interaction Playwright suite passed: 5 tests.
- Visual conformance validation passed.
- 100-gate validation passed: 100/100 with zero supporting failures.
- 120-gate validation passed: 120/120.

## Changes In This Pass

- Moved Parquet schema aliases and candidate normalization from `model_dashboard/data_loader.py` into `model_dashboard/data/transforms.py`.
- Moved diagnostic audit loading, diagnostic frame construction, and diagnostic ACF source-table construction into `model_dashboard/data/diagnostics.py`.
- Updated schema/data/gate validation scripts to consume `model_dashboard.data.config`, `model_dashboard.data.locate`, and `model_dashboard.data.transforms` directly.
- Tightened governed file discovery so generated `artifacts/`, caches, `.venv`, and test-output paths are ignored during recursive data lookup.
- Removed the whole repo root from default data search roots; only explicit data roots, `data/`, and the mini fixture fallback remain.
- Updated browser tests for the current Parquet-backed finalist wording and fixed the Streamlit Deploy visibility smoke check to inspect visible text nodes.
- Tightened verifier compile scope to avoid compiling the virtualenv or generated outputs.

## Open Backlog Items

- Continue splitting `model_dashboard/data_loader.py` into `data/parquet_loader.py`, `data/legacy_loader.py`, and `data/chart_sources.py`; config, locate, manifest, transforms, and diagnostics are materially split so far.
- Re-run the consolidated verifier against the external active diagnostic audit pack after the next split step.
- Inspect regenerated screenshots from the external active data pack before marking the broader visual/data governance objective complete.

## Next Exact Command

```powershell
rg -n "^def |^class " model_dashboard\data_loader.py model_dashboard\data
```

## Next Defect To Fix

Move Parquet dashboard orchestration out of `model_dashboard/data_loader.py` into `model_dashboard/data/parquet_loader.py`, preserving the current `DashboardData` contract and mini-fixture verifier pass.
