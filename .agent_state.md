# Agent State

Status: IN PROGRESS
Current loop number: repo-cleanup pass 1 of 80
Passed gates: not re-run after repo hygiene refactor
Failed gates: unknown until the next verifier run

## Current Scope

The active goal is repo cleanup plus governance hardening. The dashboard was previously visually and functionally close, but completion now requires a Parquet-first repository contract, no hard-coded runtime roots in core/scripts, generated artifacts removed from source control, small fixtures for CI, consolidated verifier coverage, and Playwright validation.

## Passed Gates

- Existing dashboard browser and 120-gate evidence exists from the prior sprint, but that evidence predates the current cleanup refactor.
- Python compile passed for `app.py` and `model_dashboard` after the first Parquet-first loader commit.

## Failed Gates

- Not yet revalidated after the current cleanup edits.

## Open Backlog Items

- Finish replacing stale run-folder assumptions in tests, scripts and docs.
- Add and validate `tests/fixtures/mini_parquet`.
- Remove generated artifacts from tracked source and rely on verifier regeneration.
- Run `scripts/verify_dashboard.ps1` with the active Parquet data root and inspect screenshots.

## Next Exact Command

```powershell
python -m compileall app.py model_dashboard scripts
```

## Next Defect To Fix

Continue replacing legacy run-folder performance and feature-completeness tests with Parquet-backed fixture or active-data-root checks.
