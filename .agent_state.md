# Agent State

Status: VERIFIED_IN_PROGRESS_FY2026_REVENUE_OUTLOOK_JUNCTION

Latest verified commit: `22d6d31` (`feat: complete Revenue Outlook hybrid source pack`) on `origin/main`.

Active task: FY2026 junction fix in the existing Revenue Outlook, with annual
actual completeness metadata and chart treatment separated from forecast paths.

## Current State

- Git HEAD is on `main` at the latest pushed Revenue Outlook hybrid delivery
  (`22d6d31`). The current worktree contains the scoped FY2026 junction edits
  until they are validated, committed and pushed.
- Streamlit on port 8515 returned healthy during the latest audit; pid file
  `.streamlit_8515.pid` contained `85548`.
- Revenue source-pack artifacts, promoted Revenue Outlook pack, screenshots,
  reconciliation report, remaining decisions, and source-gap register are present.
- `release_values.csv`, `forecast_archive.csv`, `quarterly_actuals.csv`,
  `fed_rate_paths.csv`, `mot_error_bands.csv`, `official_befu25_annual.csv`,
  `ped_bridge_inputs.csv`, and loader-derived audit exports are repo-local.
- `annual_completeness_audit.csv` is being added as a loader-derived audit to
  classify FY2026 as actual-to-date rather than a complete annual actual.
- `ped_bridge_inputs.csv` vendors population, total VKT, source-backed PED
  litres intensity, and forecast-input population rows. The PED bridge is no
  longer reported as a missing source gap.
- `hybrid_annual_revenue.csv` now carries FED-path variants and bridge-input
  rows. Current planned path vs no-2027-12c-uplift changes PED/FED and Total
  NLTF values from source-backed FED rate rows. Crown top-up Include/Exclude is
  applied when governed annualized top-up rows exist.
- The materializer was refactored away from direct `ws.cell` / max-row /
  max-column scans for the raw workbook exports; a temporary streaming-output
  check produced identical normalized hashes to the current pack.
- The Revenue source horizon control now supports `Next 5 FY`, `To FY2031`,
  and `Full common horizon`. Total-path traces are capped by the common
  fixed-MOT/replacement-line horizon; selected-FY audit tables remain tied to
  Selected FY rather than the forecast horizon.
- The Revenue source validation/manifest expander now includes a selectable
  component/deduction long-form table and download. The view carries signs,
  signed values, units, FED path/source provenance, source file, replacement
  flag, and availability status.
- `validation_issues.csv` now contains only the expected
  `revenue_basis_alias` and `series_registry_gap` warnings. The stale
  `unresolved_critical_decisions` warning was removed by validating against the
  generated decision-handoff statuses.
- BUG_BACKLOG.md: no unchecked items.

## Latest Passed Checks

- Focused Revenue source tests:
  `tests/test_revenue_source_pack.py`,
  `tests/test_revenue_source_app_views.py`: `39 passed`
  (`pytest-revenue-source-replacement-invariants`).
- Focused horizon tests:
  `test_revenue_source_horizon_control_limits_path_chart_without_selected_fy_coupling`
  and `test_full_common_horizon_stops_at_last_fixed_or_replacement_row`: PASS.
- Focused component/deduction long-form test:
  `test_component_deduction_long_form_is_selectable_signed_and_download_ready`: PASS.
- Non-browser verifier:
  `scripts\verify_dashboard.ps1 -Python python -Port 8515 -ReuseExistingServer -CommandTimeoutSeconds 900 -SkipBrowser`
  through `scripts\invoke_bounded.ps1`: PASS
  (`verify-dashboard-skip-browser-requirement-audit`).
- Host browser verifier:
  `scripts\verify_browser_host.ps1 -Python python -Port 8515 -CommandTimeoutSeconds 900`
  through `scripts\invoke_bounded.ps1` outside the sandbox: `38 passed`
  (`host-playwright-requirement-audit`).
- Local path leak check over changed Revenue source outputs/code: no
  user-profile or cloud-drive absolute path strings.
- Revenue Outlook geometry evidence:
  `artifacts/screenshots/revenue_outlook_geometry_after.json`,
  `artifacts/screenshots/revenue-outlook-after-1680x940.png`, and
  `artifacts/screenshots/revenue-outlook-after-820x940.png`.
- Requirement audit evidence:
  `artifacts/revenue_outlook_requirement_audit.md`.
- Focused FY2026 junction tests:
  `tests/test_revenue_source_pack.py`,
  `tests/test_revenue_source_app_views.py`: `41 passed`
  (`pytest-fy2026-junction-focused`).

## Remaining Work

- Run the full required validation set for the FY2026 junction update, capture
  chart evidence, commit the scoped changes, push `main`, and confirm clean
  status.
- Light/Heavy RUC revenue is replayed from net-km rows and source-derived
  effective rates. Independent nominal effective-rate tables remain a
  provenance limitation, but the current handoff marks the runtime replay
  source-backed rather than open.
- H13+ treatment is recorded as label-applied with no value changes.
- Do not mark the goal complete until every explicit objective requirement has
  authoritative current evidence.
