# Agent State

Status: VERIFIED

- Latest verified commit: `20020cb` before this candidate-cone repair pass.
- BUG_BACKLOG.md: no unchecked items.

Active repair status: LOCALLY VALIDATED

Active task: repair Candidate Search Frontier so PED, Light RUC and Heavy RUC
clouds form optimisation cones around the current finalists.

Current outcome:

- Confirmed root cause in `scripts/promote_vnext_to_evidence_pack.py`:
  old `rebuild_candidate_cone` replaced only legacy finalist anchor rows and
  left the 400 surrounding frontier rows unchanged.
- Added `scripts/regenerate_candidate_cone.py`, an importable deterministic
  builder with `--check`, `--dry-run`, `--seed`, atomic writes, backup support,
  manifest/inventory refresh helpers, row-class provenance and geometry audit
  metrics.
- Wired `scripts/promote_vnext_to_evidence_pack.py` to call the new builder
  instead of the old anchor-only row swap.
- Added `tests/test_candidate_cone_builder.py` covering exact counts/schema,
  current finalist and Schiff anchors, deterministic repeatability, Light RUC
  measured-row handling, no non-anchor row undercutting the finalist apex under
  paper or operational basis, and basic cone geometry spread.
- Rebuilt `data/dashboard_evidence_pack/data/candidate_cone.parquet` so each
  stream has the current finalist as the visual apex. Light RUC now uses 68
  measured candidate rows and 66 deterministic display-fill rows, plus the
  finalist and Schiff anchors.
- Confirmed the saved Light RUC frontier has zero non-anchor points below or
  left of the finalist under both paper-style and operational MAPE axes.

Commands run:

- `python -m compileall app.py model_dashboard\ui.py` passed.
- `python -m compileall scripts\regenerate_candidate_cone.py tests\test_candidate_cone_builder.py tests\test_chart_source_tables.py` passed.
- `python -m pytest -q tests\test_candidate_cone_builder.py tests\test_chart_source_tables.py` passed: 13 tests.
- `python scripts\regenerate_candidate_cone.py --check` passed under the
  scratch pyarrow 24 runtime.
- `python scripts\validate_dashboard_data.py --data-root data\dashboard_evidence_pack` passed under the scratch pyarrow 24 runtime.
- `python scripts\validate_chart_sources.py --data-root data\dashboard_evidence_pack` passed under the scratch pyarrow 24 runtime.
- `python scripts\validate_semantic_labels.py --data-root data\dashboard_evidence_pack` passed under the scratch pyarrow 24 runtime.

Important local environment note:

- Do not retry long-running package installs without explicit approval and a
  tight timeout. The previous `pip install --target ... fastparquet` attempt
  hung and was manually stopped. The remaining runaway conda Python process
  from that attempt was killed.
- The local `.runtime_pyarrow24` folder was a scratch validation dependency
  folder only and must not be committed.

Not completed from the broader earlier goal:

- Full `pytest`, `scripts\verify_dashboard.ps1`, and Playwright screenshot
  regeneration were not completed green in this repair pass. Earlier full-gate
  runs still had failures outside the candidate-frontier fix scope.
