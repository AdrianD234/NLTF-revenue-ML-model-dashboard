# Generated Databricks App runtime bundle

Do not edit this branch by hand. It is regenerated by the
'Publish Databricks App bundle' workflow on the main repository.

- Source main SHA: `e14654ab9ea99c5de8ce33ee5b810c99ba7b9b6a`
- Bundle manifest SHA-256: `20373d7d420ac92a3faef498dfa3b3dc5b939ad8d8746b312eb4836f8bbc83e7`
- Built (UTC): 2026-08-06T23:23:23Z

Databricks App deployment settings: branch `databricks-app`,
source code path `app`.

## About this first publication

This initial commit was produced locally by the same
`scripts/build_databricks_app_bundle.py` and
`scripts/validate_databricks_app_bundle.py` that the workflow runs, from the
merged main commit above, and it passed the full bundle validator including
bundle-vs-source runtime value parity.

It was published by hand only because GitHub Actions could not start any job
at the time:

    The job was not started because recent account payments have failed or
    your spending limit needs to be increased.

Once that billing issue is resolved, every push to `main` refreshes this
branch automatically; no manual step is required. The layout here (`README.md`
plus `app/`) is exactly what the workflow produces, so its ownership guard
will accept and overwrite this branch.
